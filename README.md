# Getting started

- docker compose up --build

## Subsequently

- docker compose up -d

### Update dependencies 

- In the VM `docker compose run web poetry install`

## When finished

- docker compose down
